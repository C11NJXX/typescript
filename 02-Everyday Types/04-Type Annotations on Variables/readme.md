```
TypeScript doesn’t use “types on the left”-style declarations like int x = 0; Type annotations will always go after the thing being typed.
TypeScript 不使用“左侧类型”样式的声明，例如int x = 0; 类型注释总是跟在所输入的内容之后。
```